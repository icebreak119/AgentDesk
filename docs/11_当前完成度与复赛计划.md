# AgentDesk 当前完成度与复赛计划

> 本文档用于初赛评审，明确「已完成 / 进行中 / 复赛计划」，避免过度承诺。

## 1. 总体结论

**初赛定位：设计完整 + 关键路径可运行 + 诚实标注边界。**

可对外表述：

> 抖音 Channel Runtime（8765 中文控制台）可独立启动；  
> **初赛参考编排器**（`orchestrator/`）可离线跑通剧本 A/B/C，输出包含去重、客户确认和 CaseDigest 的 `trace.jsonl`；
> 7 个 Skill 已注册并附 Schema，其中 6 个可 CLI 调用；MCP 契约含错误码/审计/幂等规范与 pytest；
> AgentTeams **官方运行时**、Trace 工作台 UI、Task DB 为复赛项。

## 2. 已完成（可举证）

| 类别 | 内容 | 证据形式 |
|---|---|---|
| 方案设计 | 6 Agent + 7 Skill + 9 步闭环 | `docs/03~05` |
| AgentTeams 映射 | Manager–TL–Worker 分层 | `06_架构图.png` |
| **参考编排器** | DutyManager / SessionTL / Workers + demo | `python -m orchestrator.demo.script_a_consult` |
| **Trace 证据** | 每步 agent→skill→status JSONL | `orchestrator/output/trace.jsonl` |
| **Skill 注册** | registry.yaml + 7 个 Schema；6 个 CLI 可运行 Skill | `python skills/run_skill.py customer_confirm ...` |
| **跨渠道去重** | 匿名客户标识、归一内容和 5 分钟窗口；重复任务 `deduplicated` | `script_c_multichannel_case` Trace |
| **客户确认与案例沉淀** | CustomerConfirm + 匿名 JSONL CaseDigest + 标签检索 `case://` | 剧本 C + pytest 隐私断言 |
| 抖音 Channel Runtime | IPC HTTP `8765` + 源码 `runtime/douyin/` | `/console` 截图 |
| 抖音私信接入 | 入站监听、会话管理 | Runtime 日志 |
| 消息发送 | send_im_message 链路 | 发送回执 / mock+`--live` |
| 发送核验约束 | profile 隔离、短文本二次校验设计 | 日志分支关键字 |
| MCP 等价契约 | errors/audit/idempotency + channel.* | `docs/contracts/` + pytest |
| 开源材料 | GitHub 设计仓 + 源码 | icebreak119/AgentDesk |

## 3. 进行中（有部分实现，未形成生产闭环）

| 类别 | 当前状态 | 说明 |
|---|---|---|
| 企微统一适配 | 设计与扩展预留 | 不作为初赛已实现主链路，复赛接入可替换渠道 adapter |
| 审批流产品化 | 参考实现 | 剧本 B 有 ApprovalToken 闸门；无独立审批 API |
| 知识注入 | 结构化标签检索可运行 | CaseLearning 按意图/风险标签注入 `knowledge_hits`；完整 RAG 体系复赛完善 |
| 编排器联调 live | 可选 | `--live` 调 8765；默认 mock 离线可跑 |

## 4. 复赛计划（明确未声称初赛完成）

| 优先级 | 任务 | 交付物 |
|---|---|---|
| P0 | AgentTeams **官方**运行时接入 | 替换 `orchestrator/` 调度层 |
| P0 | 抖音端到端 live Demo | 视频 + 真实发送全链路 |
| P1 | Task Runtime 后端 | `/tasks` API、状态机持久化 |
| P1 | 审批流后端 | ApprovalToken 签发/校验 API |
| P1 | Evidence 持久化 | verify 证据存储与检索 |
| P2 | Trace 工作台 UI | agent→skill→tool 可视化 |
| P2 | 完整 RAG 与离线评测 | 向量检索、知识治理、命中质量与成本指标 |
| P3 | 企微扩展演示 | 证明渠道 adapter 可替换 |

## 5. AI Agent vs Runtime Agent（完成度说明）

### AI Agents（编排层）

| Agent | 初赛 | 证据 |
|---|---|---|
| DutyManager | ✅ 参考实现 | `orchestrator/agents/duty_manager.py` |
| SessionTL | ✅ 参考实现 | 状态机 + `trace.jsonl` |
| ChannelIngress / TriageGuard / ActVerify / CaseLearning | ✅ 参考 Worker | `orchestrator/agents/workers/` |
| AgentTeams 官方包 | 🔄 复赛 | 替换调度层，Skill/契约不重写 |

### Runtime Agent（执行层）

- Windows 本地执行节点、浏览器托管、抖音发送执行
- 初赛：真实渠道执行能力 ✅
- 复赛：统一 MCP Server 包装 8765 HTTP 🔄

## 6. Trace 说明

**初赛状态：参考编排器输出 JSONL，非 Web 工作台。**

已实现：

- `orchestrator/output/trace.jsonl` — 剧本 A/B 可复现
- `orchestrator/output/trace_c_multichannel.jsonl` — 剧本 C 可复现
- 字段含 `task_id / agent / skill / status / event / mode`，并记录去重、确认、案例引用等关键事件

复赛建设 Trace 工作台 UI，读取同一 trace 格式。

## 7. 风险与规避

| 风险 | 规避方式 |
|---|---|
| 评委误以为官方 AgentTeams 已接入 | README + 本文档标注「参考实现」 |
| mock 发送被质疑 | `--live` + 8765 截图；trace 标注 `mode` |
| 渠道叙事混乱 | 不使用无关渠道或历史项目截图 |
| Demo 链接不可用 | 提交公网视频 URL |

## 8. 初赛自检（补强后）

- [x] `orchestrator/demo` 剧本 A/B/C 可一键运行
- [x] `skills/run_skill.py` 6 个独立 Skill 有样例输出
- [x] `python -m pytest -q` 全绿（56 passed）
- [x] `trace.jsonl` ≥ 5 行跨 Agent 证据
- [ ] Trace Web UI（复赛）
- [ ] AgentTeams 官方运行时（复赛）
